// https://discord.com/developers/docs/resources/guild#get-guild-query-string-params

export interface RawGuildParams {
	/** when true, will return approximate member and presence counts for the guild	DEFAULT: false */
	with_counts?: boolean;
}
